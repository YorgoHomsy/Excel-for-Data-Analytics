Excel formulas 
-
use math operators (+ - * / ^) and comparison operators (= > < >= <= <>)

Excel follows operator precedence; use parentheses to control calculations

Autofill adjusts cell references automatically

Cell References
-
Relative:

A1 → updates row and column

Absolute (Fixed): $A$1 → locks row and column

Mixed:

$A1 → column locked

A$1 → row locked

Shortcut: F4 cycles through reference types
